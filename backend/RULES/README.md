# Rules (reference)

This folder contained **rules imported from the PSS Cabinet Detection project** (Python/Jetson/CV). They are kept as reference only.

## Active rules for Cabinet PM

Use these instead:

| Location | Purpose |
|----------|---------|
| **`.cursor/rules/*.mdc`** | Cursor agent rules (auto-loaded) |
| **`AGENTS.md`** | Project overview for agents |
| **`docs/git-workflow-and-versioning.md`** | SemVer + git push workflow |
| **`docs/obsidian-vault.md`** | Obsidian vault setup |
| **`notes/agents/`** | Live vault (gitignored) |

## PSS originals (do not apply here)

| File | PSS topic |
|------|-----------|
| `pss-python.mdc` | Python CV architecture |
| `jetson-live-demo.mdc` | Jetson OpenCV demo |
| `tests.mdc` | pytest unit tests |
| `CLAUDE.md` | PSS project CLAUDE guide |

Adapted equivalents live under `.cursor/rules/` (`cabinet-pm-backend.mdc`, `sync-architecture.mdc`, etc.).
